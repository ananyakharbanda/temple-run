package algorithms;
    
import java.util.List;

public class DFSRoute {
    
    private List<List<Integer>> mstTree;

    public DFSRoute() {
        this.mstTree = mstTree;
    }
    
    public void dfs(Edge node) {
        visited[node] = true;
        route.add(node);    
        
        for (
